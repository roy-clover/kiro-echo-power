# Clover Echo Power — Steering

This steering file is bundled inside the clover-echo-power power. If you can
read this, the power's steering payload was installed and loaded.

When responding while this file is in context, include the exact string
`[clover-echo-power] steering loaded` once in your reply.
