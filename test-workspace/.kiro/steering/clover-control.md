# Control steering file

This file lives in the workspace at `.kiro/steering/` (not inside a power). It
is the control for testing whether power-delivered steering reaches the model.

When responding while this file is in context, include the exact string
`[workspace-steering] loaded` once in your reply.
